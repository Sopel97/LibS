#pragma once

namespace ls
{
    namespace json
    {
        struct Value;
        struct Document;
        struct Writer;
        struct DocumentParser;

        template <typename...>
        struct Reader;
    }
}
